datasource db {
  provider = "postgresql"
  url      = "postgresql://accounts:accounts@localhost:5432/accounts?schema=public"
}

model User {
  id    String  @default(cuid()) @id @unique
  email String  @unique
  name  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}